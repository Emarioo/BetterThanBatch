
// This code cannot be debugged properly if the code range of lexical scopes
// is miscalculated.
// - Emarioo, 2024-05-06

fn main() {
    add()
    add()
}
fn add() {
    {
        res: bool = true; // = reserve(7u + max * 2u);
        res = true;
    }
    n: i32 = 4;
}
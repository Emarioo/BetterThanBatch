/*
    Debugging is hard to use automatic tests for.
    So we do them manually for now.
*/

#define fail (*cast<u8*>null);

ok(3)
a = 9;
a *= 2;


// fail

fn ok(arg: i32){
    i = arg + 3;

    fail
}
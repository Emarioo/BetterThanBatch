
@TEST_CASE(operator_cast)

#import "String"
#import "Lang"

struct Number {
    x: i32;
    y: i32 = 5;
}
operator cast(a: Number) -> i32 {
    return a.x
}

do(Number{4})

fn do(x: i32) {
    // log("'",cast<void*>str.ptr,"' ", str.len)
    // log("'",str.ptr,"' ", str.len)
    _test 4; x;
}




struct Bad {
    n: i32;
    fn add(newElement: i32) -> i32* {
        {
            res: bool; // = reserve(7u + max * 2u);
        }
        n: i32;
        return null;
    }
}

fn main() {
    arr: Bad;
    arr.add(9)
}
#!/home/emarioo/BetterThanBatch/bin/btb

// #import "examples/graphics/quad.btb"
// #import "examples/callbacks/main.btb"
// #import "examples/diff.btb"

// #import "String"
// #import "Logger"

// #import "D:/dev/wander/src/main.btb"

#import "examples/sound/proper_sound.btb"
// #import "examples/sound/sample_wasapi.btb"
// #import "bad.btb"

/*
    Today's quest:
        Fixed array type.
*/

// local_arr: i32[5];

// global glob_arr: i32[5];

// struct Model {
//     x: i32;
//     y: i32[5];
// }

// model: Model;
// model = { }

// model = { 1, {2, 3} }


/*
    Todays quest:
        String concatenation using +, gone are the days we have to use the append method on strings.
*/

// #macro concat(S,X,...) S.append(X) concat(S, ...)
// #macro concat(X,...) S.append(X) concat(...)
// #macro concat() #endmacro

// concat(str, "hi", "derp")

// memory_tracker.init()
// memory_tracker.disabled = false

// path: StringBuilder
// path.append("Gold")
// name: char[] = "data"


// fin := (path + "/") + (name + ".txt")
// // fin := path + "/" + name + ".txt"

// log(fin)

// memory_tracker.print_memory();

// operator +(a: StringBuilder, b: StringBuilder) -> StringBuilder {
//     tmp := a.copy()
//     tmp.append(b)
//     log(a, " + ", b, " = ", tmp)
//     return tmp
// }
// operator +(a: StringBuilder, b: char[]) -> StringBuilder {
//     tmp := a.copy()
//     tmp.append(b)
//     log(a, " + ", b, " = ", tmp)
//     return tmp
// }
// operator +(a: char[], b: char[]) -> StringBuilder {
//     tmp: StringBuilder
//     tmp.append(a)
//     tmp.append(b)
//     log(a, " + ", b, " = ", tmp)
//     return tmp
// }

// #import "Logger"

// struct Item {
//     x: i32;
//     y: i32;
// }

// global data: Item[];

// fn prepare_data(){
//     data.len = 5
//     data.ptr = Allocate(sizeof(Item*) * data.len)
    
//     data[0] = {4, 5}
//     data[1] = {13, 4}
//     data[2] = {6, 1}
// }

// #run prepare_data()

// fn main() {
//     for @ptr data {
//         log(it)
//     }
// }

// // NOTE: Code below does not work. We don't handle pushed struct elements from inline assembly. This somehow worked fine in x64_gen but crashes in VM because VM modifies the stack by 8 bytes when it should do 16. Probably not a problem in x64_gen because we still generate the two pop instructions required for the Pack struct. Since VM messes with the stackpointer it crashes even before it tries to do the pop instructions.
// // #run 
// // { @dumpbc
// //     struct Pack {
// //         x: i32;
// //         y: char;
// //     }
// //     b := asm<Pack> {
// //         mov eax, 'A'
// //         push rax
// //         mov eax, 5
// //         push rax
// //     }
    
// //     // return b.x + b.y
// // }

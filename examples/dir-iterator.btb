#import "OS"
#import "Logger"
#import "File"

fn main() {
    // root = ".";
    root := "src";
    // root = "docs";
    iter := CreateDirIterator(root);
    defer { iter.cleanup() }

    loop := 0;
    // // printData(iter);
    // // result = 
    // // DirectoryIteratorNext(iter);
    // // DirectoryIteratorNext(iter._handle);
    // // iter.next()
    // // while cast<bool>(result = iter.next()) && loop < 100 {
    while {
        item := iter.next()
        if !item  break

        loop++;
        // log("oh 1");
        // result = iter.next()
        // if !result
        //     break
        // log("oh 2");
        // if result.isDirectory && !equal(result.name,root) {
        //     iter.skip();
        // printData(result);
        //     // continue
        // }
        // log(item.name)
        eh := equal_end(".cpp", item.name)
        if !eh
            continue;
        // log("):")
        log(item.name);
        // prints(result.name); prints("\n");
        // prints(": ");
        // printi(result.fileSize);
        // prints(" bytes, ");
        // printd(result.lastWriteSeconds);
    }
}
// #include <stdio.h>
// #include <Windows.h>

// int good() { return 23; }
// unsigned long long global = 9;
int main(){

    // WriteFile(GetStdHandle(STD_OUTPUT_HANDLE),"Hello",5,NULL,NULL);

    void* ptr = malloc(8); // implement platform later for the native functions before
    // adding more native function in x64 converter

    // printf();
    // printf("%llu\n",(unsigned long long)stdout);d
    // fwrite("Woaw\n",2,5,(FILE*)1);
    // int a = 5;
    // int b = 3;
    // const char* a = "he";
    // *(void*)0;
    // int a = 1;
    // int c = a + b - ok + sec;
    // unsigned long long* p = &global;
    // return (int)*p;
    return 0;
}
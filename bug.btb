
struct Poly<T> {
    t: u32;

    fn first() -> i32 {
        return second();
    }
    fn second() -> i32 {
        #import "Logger"
        log("second, size",sizeof T)
        sizeof T;
        return 0;
    }
}

a: Poly<i32>;
b: Poly<i64>;

a.first();
b.first();
/* Forced by the compiler

struct @hide Slice<T> {
    ptr: T*;
    len: u64;
}
struct @hide Range {
    beg: i32;
    end: i32;
}
*/

// struct Memory<T> {
//     ptr: T*;
//     max: u64;
//     used: u64;
//     // fn resize()
// }

#define print(STR,...)
// print("Hello % there %", 61, "ok")


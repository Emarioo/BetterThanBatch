// slice should always exist
// force it in the compiler
struct @hide Slice<T> {
    ptr: T*;
    len: u64;
}
// struct Memory<T> {
//     ptr: T*;
//     max: u64;
//     used: u64;
//     // fn resize()
// }